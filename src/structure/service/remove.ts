import { Round } from '../../round';

export class StructureRemoveService {

    constructor(
        private round: Round
    ) {

    }

    // emptyQualifyGroups() {
    //     this.round.getQualifyGroups(Round.WINNERS).empty();
    //     this.round.getQualifyGroups(Round.LOSERS).empty();
    // }
}